rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      // WARNING: Temporarily open for debugging data seeding issues.
      allow write: if true; 
    }
  }
}